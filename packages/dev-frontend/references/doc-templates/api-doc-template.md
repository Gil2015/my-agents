# 接口文档（api.md）

- 模块名：
- 模块显示名：
- 需求来源：

## API-001

- 方法：GET
- 路径：/api/example
- 描述：
- 关联需求：REQ-001
- 调用场景：
- 接口状态：DRAFT
- 鉴权：无
- 负责人：

### 请求参数

1. `id` (string, 必填) - 说明

### 响应字段

1. `code` (number, 不可空) - 说明
2. `data` (object | null) - 说明

### 错误码

1. `4001` - 含义：；前端处理策略：

### Mock 值

```json
{

}
```

## 合并规则

- 同一模块的多次联调产出应增量追加到同一份 `api.md`，不另建文件。
- 已有 `API-*` 条目只更新变更字段，不删除或重排编号。
- 新增接口使用下一个可用的 `API-*` 编号。
- 接口状态变更（如 `DRAFT -> CONFIRMED`）直接在原条目上修改，不新建条目。
