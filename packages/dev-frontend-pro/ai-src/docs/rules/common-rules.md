# dev-frontend-pro 通用规则

- 先确认 mission，再进入阶段执行。
- 任务产物写入 `.ai-src/dev-frontend-pro/missions/{missionId}`。
- 项目级长期资料写入 `.ai-src/dev-frontend-pro/docs` 或 `.ai-src/dev-frontend-pro/templates`。
- 不自动提交代码。
- 不凭猜测补齐需求、接口或业务规则。
- 读取复杂历史模块前先查索引；必要时只读切片。
