# 历史模块读取规则

复杂业务模块默认只作为模式参考。

## 顺序

1. 读取 `module-catalog.md`。
2. 读取模板规则和项目级规则。
3. 只在必要时读取 1-2 个相关历史模块切片。

## 切片优先级

- 入口文件。
- 类型定义。
- 组件导出。
- README 或 rule。
- 与当前需求直接相关的 hook 或布局文件。

## 禁止

- 不全量读取复杂业务模块。
- 不照搬业务逻辑。
- 不让历史模块覆盖当前需求。
